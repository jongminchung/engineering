type: object
required:
  - id
  - name
properties:
  id:
    type: integer
    format: int64
  name:
    type: string
  category:
    $ref: ../../petstore.yaml#/components/schemas/Category
  tags:
    type: array
    items:
      $ref: ../../petstore.yaml#/components/schemas/Tag
