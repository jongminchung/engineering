# Platform Engineer

_**Table of Contents**_

<!-- TOC -->
* [Platform Engineer](#platform-engineer)
  * [API First](#api-first)
    * [RESTful API](#restful-api)
    * [API-First Design](#api-first-design)
    * [OpenAPI](#openapi)
    * [버전 관리](#버전-관리)
    * [API 개발 흐름 구축](#api-개발-흐름-구축)
      * [API 개발 흐름이 왜 중요한가?](#api-개발-흐름이-왜-중요한가)
    * [API 표준](#api-표준)
    * [현대적인 서비스 통신방식(REST, gRPC등)에 대한 이해를 바탕으로 확장성과 성능, 그리고 보안을 고려한 API 설계](#현대적인-서비스-통신방식rest-grpc등에-대한-이해를-바탕으로-확장성과-성능-그리고-보안을-고려한-api-설계)
  * [SDK, Terraform, CLI 자동 생성할 수 있는 개발 흐름(CI/CD 환경, 시스템 연동)](#sdk-terraform-cli-자동-생성할-수-있는-개발-흐름cicd-환경-시스템-연동)
    * [AWS Smithy](#aws-smithy)
    * [OpenAPI Generator](#openapi-generator)
  * [CLI](#cli)
    * [설계](#설계)
      * [전체 아키텍처](#전체-아키텍처)
  * [gRPC](#grpc)
    * [Protobuf 방식 통신 HTTP1.1](#protobuf-방식-통신-http11)
  * [개발자 경험(DX)을 향상시키는 다양한 도구 (Java, Go, Typescript, Python 등 다양한 언어)](#개발자-경험dx을-향상시키는-다양한-도구-java-go-typescript-python-등-다양한-언어)
  * [Kubernetes API Conventions](#kubernetes-api-conventions)
    * [`read-modify-write` 왜 GET -> PUT에서 문제가 생기나](#read-modify-write-왜-get---put에서-문제가-생기나)
      * [낙관적 락으로 해결](#낙관적-락으로-해결)
    * [PATCH 메서드](#patch-메서드)
      * [1. JSON Patch (RFC 6902)](#1-json-patch-rfc-6902)
      * [2. JSON Merge Patch (RFC 7396)](#2-json-merge-patch-rfc-7396)
      * [3. Strategic Merge Patch](#3-strategic-merge-patch)
<!-- TOC -->

- OpenAPI 스펙을 기반으로 API를 설계하고,
  이를 바탕으로 SDK, Terraform, CLI 등을 자동 생성할 수 있는 개발 흐름(CI/CD 환경, 내부 시스템 연동)
    - API 표준 연구 (OpenAPI Spec 기반으로 아티팩트가 정의됨)
        - [K8s API convention](https://github.com/kubernetes/community/blob/master/contributors/devel/sig-architecture/api-conventions.md)
- 개발자 경험(DX)를 향상시키는 다양한 라이브러리 개발

---

## API First

API 스펙(OpenAPI/Proto)을 ‘산출물 문서’가 아니라 ‘공유 계약(contract)’이자 ‘단일 진실의 원천(SoT)’으로 운영

1. 스펙이 코드보다 먼저 변경됨
2. 스펙은 "계약"이라 계약 참여자가 모두 참여애햐함
3. 스펙으로 생성물(gen code)는 계약 참여자가 특정 버전에 맞춰서 계약에 맞춰 비즈니스 로직 구현
4. API 스펙 표준 컨벤션 린팅 관리
    - OAS Linting
    - Team Spec Linting
        - 예시: Custom Header, Response 구조.
5. API 스펙 이외에도 API 문서를 통해서 구체적으로 설명 제공 (Redocly, Docusaurus 등등..)

### RESTful API

자원 기반 설계, HTTP 메서드의 올바른 사용, 상태 코드 설계 등.

### API-First Design

코드부터 작성하는 것이 아니라 스펙을 먼저 정의함.

이를 통해 팀간 협의에서 해당 API 스펙으로 기획적으로 설계하고, 개발자와 사용자 간의
의사소통을 단순화하여 API의 유지보수와 확장성을 향상시킵니다.

### OpenAPI

- [components](https://swagger.io/docs/specification/v3_0/components/#:~:text=type%3A%20string-,Components%20Structure,-components%20serve%20as)
- [securitySchemes](https://swagger.io/docs/specification/authentication/)

### 버전 관리

RESTful 원칙을 엄격히 지켜야 하는 내부 시스템 간 통신에는 Header 방식이 우아할 수 있지만,
수많은 외부 개발자가 이용하는 퍼블릭 클라우드 API 특성상 가독성, 캐싱 효율, 디버깅 편의성이 뛰어난 URI 방식이 현실적으로 더 안정적이고
친화적인 선택이라고 생각한다.

- 새 버전을 낼 때 구 버전을 일정 기간 유지(Deprecation Period)하고, SDK 단에서 경고(Warning)를 띄워주는 자동화
  흐름을 구축.

### API 개발 흐름 구축

"누가 만들어도 동일한 품질과 규격을 유지하면서, 설계부터 배포까지의 과정을 자동화된 컨베이어 벨트처럼 만드는 시스템"을 구축

- OpenAPI Spec이라는 설계도를 넣으면, 고품질의 API 서버, SDK, 테라폼, 문서가 자동으로 쏟아져 나오는 공장을 짓는 일->

**제안:**

- Gitlab CI 단계에서 OpenAPI Spec 사내 표준에 맞는 지 검증 단계를 추가
    - OAS 자체의 Linting은 Redocly
- mock server 자동생성
    - openapi-spec이 확정되면 바로 호출 가능한 가짜 서버를 즉시 띄워주는 파이프라인
    - example을 기반으로
- API가 바뀌었을 때 사람이 일일이 SDK나 문서를 수정하지 않게 하는 과정

#### API 개발 흐름이 왜 중요한가?

1. 확장성(Scalability): 개발자가 10명일 때와 100명일 때 API 품질이 달라지면 안 된다.
   흐름이 구축되어야 조직이 커져도 일관된 품질을 유지할 수 있다.
2. 민첩성(Agility): 수작업을 줄여 API 설계 후 실제 SDK가 개발자의 손에 들리기까지의 시간을 최소화하는 것이 목표이다.
3. 신뢰성(Reliability): 사람의 실수가 개입할 틈이 없는 자동화된 검증 흐름을 통해 클라이언트에게 안정적인 서비스를 보장하기
   위함이다.

### API 표준

### 현대적인 서비스 통신방식(REST, gRPC등)에 대한 이해를 바탕으로 확장성과 성능, 그리고 보안을 고려한 API 설계

---

## SDK, Terraform, CLI 자동 생성할 수 있는 개발 흐름(CI/CD 환경, 시스템 연동)

- OpenAPISpec -> OpenAPI Generator -> SDK

| 단계             | **AWS Smithy**               | **Google GAPIC**            | **MS TypeSpec**         | **OpenAPI Generator** |
|----------------|------------------------------|-----------------------------|-------------------------|-----------------------|
| **스펙 형식**      | Smithy IDL (.smithy)         | Protobuf (.proto)           | TypeSpec (.tsp)         | OpenAPI (yaml/json)   |
| **1️⃣ Parser** | 자체 파서                        | protoc                      | 자체 TypeSpec 컴파일러        | swagger-parser        |
| **2️⃣ 중간 표현**  | Semantic Model (Shape Graph) | FileDescriptor → API Schema | TypeSpec Semantic Model | CodegenModel          |
| **3️⃣ 템플릿**    | 언어별 Codegen 모듈               | Jinja2                      | Emitters (플러그인)         | Mustache/Handlebars   |
| **확장성**        | Traits로 메타데이터 확장             | Options/Annotations         | Decorators              | x-extensions          |

### AWS Smithy

```text
┌─────────────────────────────────────────────────────────────────────┐
│                         AWS Smithy Pipeline                         │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────┐     ┌─────────────────┐     ┌─────────────────┐   │
│  │ .smithy     │────▶│  Semantic Model │────▶│  smithy-rs     │   │
│  │ .json (AST) │     │  (Shape Graph)  │     │  smithy-go     │   │
│  └─────────────┘     └─────────────────┘     │  smithy-python │   │
│                              │               └─────────────────┘   │
│                              ▼                       │              │
│                      ┌─────────────┐                 ▼              │
│                      │   Traits    │         Generated Code         │
│                      │ (메타데이터) │         • SDK Client           │
│                      │ @http       │         • Server Stubs         │
│                      │ @paginated  │         • CLI                  │
│                      │ @auth       │                                │
│                      └─────────────┘                                │
└─────────────────────────────────────────────────────────────────────┘
```

**핵심 특징:**

- **Semantic Model**: 프로토콜 독립적인 API 추상화
- **Traits**: 확장 가능한 메타데이터 시스템 (HTTP 바인딩, 페이지네이션 등)
- **다중 표현**: IDL과 JSON AST 간 상호 변환 가능

### OpenAPI Generator

```text
┌─────────────────────────────────────────────────────────────────────┐
│                    OpenAPI Generator Pipeline                        │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────┐     ┌─────────────────┐     ┌─────────────────┐   │
│  │ openapi.yaml│────▶│ swagger-parser  │────▶│  CodegenModel   │   │
│  │ openapi.json│     │                 │     │  (Java Objects) │   │
│  └─────────────┘     └─────────────────┘     └─────────────────┘   │
│                                                      │              │
│                                                      ▼              │
│                              ┌──────────────────────────────────┐   │
│                              │  Language-specific Generator     │   │
│                              │  ┌────────────────────────────┐  │   │
│                              │  │ TypeScriptFetchGenerator   │  │   │
│                              │  │ GoClientGenerator          │  │   │
│                              │  │ PythonClientGenerator      │  │   │
│                              │  │ ... (50+ generators)       │  │   │
│                              │  └────────────────────────────┘  │   │
│                              └──────────────────────────────────┘   │
│                                              │                      │
│                                              ▼                      │
│                              ┌──────────────────────────────────┐   │
│                              │  Mustache Templates              │   │
│                              │  ┌────────────────────────────┐  │   │
│                              │  │ {{#operations}}            │  │   │
│                              │  │   func {{operationId}}()   │  │   │
│                              │  │ {{/operations}}            │  │   │
│                              │  └────────────────────────────┘  │   │
│                              └──────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

**핵심 특징:**

- **Java 기반**: 전체 파이프라인이 Java로 구현
- **Mustache 템플릿**: Logic-less 템플릿 (조건/반복만 가능)
- **Generator 상속 구조**: 언어별 Generator가 공통 로직 상속

## CLI

내부적으로 SDK를 사용하더라도 이를 감싸는 Wrapper로 제공 하는 식으로 제공됨.

```text
# 1️⃣ 정적 완성 - 명령어 목록 (코드에 정의됨)
$ kubectl <TAB>
get  delete  apply  describe  ...   # ← 바이너리에 내장

# 2️⃣ 정적 완성 - 리소스 타입 (코드에 정의됨)
$ kubectl get <TAB>
pods  services  deployments  ...    # ← 바이너리에 내장

# 3️⃣ 정적 완성 - 플래그 옵션 (코드에 정의됨)
$ kubectl get pods -o <TAB>
json  yaml  wide  table  ...        # ← 바이너리에 내장

# ─────────────────────────────────────────────────────

# 4️⃣ 동적 완성 - Pod 이름 (API 호출!)
$ kubectl get pods <TAB>
nginx-abc123  redis-xyz789  my-app-456  ...  # ← K8s API에서 가져옴

# 5️⃣ 동적 완성 - 네임스페이스 (API 호출!)
$ kubectl get pods -n <TAB>
default  kube-system  production  staging  ...  # ← K8s API에서 가져옴

# 6️⃣ 동적 완성 - 컨텍스트 (kubeconfig 파일에서)
$ kubectl config use-context <TAB>
minikube  prod-cluster  dev-cluster  ...  # ← kubeconfig 파일에서 읽음
```

### 설계

#### 전체 아키텍처

```text
┌─────────────────────────────────────────────────────────────────────┐
│                 IaaS CLI 자동화 아키텍처 (권장)                       │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                 1️⃣ API 스펙 (Single Source of Truth)          │  │
│  │  ┌────────────────────────────────────────────────────────┐  │  │
│  │  │  OpenAPI 3.0 + Custom Extensions (x-cli)               │  │  │
│  │  │                                                        │  │  │
│  │  │  • 서비스별 스펙 분리 (compute.yaml, network.yaml)     │  │  │
│  │  │  • CLI 메타데이터 확장 (명령어명, 플래그, 자동완성)    │  │  │
│  │  │  • 출력 테이블 컬럼 정의                                │  │  │
│  │  │  • 유효성 검증 규칙                                    │  │  │
│  │  └────────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                │                                    │
│                                │ 트리거                             │
│                                ▼                                    │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                    2️⃣ 코드 생성 파이프라인                    │  │
│  │  ┌────────────────────────────────────────────────────────┐  │  │
│  │  │  Generator (Go/Python 스크립트)                        │  │  │
│  │  │                                                        │  │  │
│  │  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐      │  │  │
│  │  │  │   CLI   │ │ SDK-Go  │ │SDK-Python│ │  Docs   │      │  │  │
│  │  │  │Generator│ │Generator│ │Generator │ │Generator│      │  │  │
│  │  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘      │  │  │
│  │  └────────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                │                                    │
│                                ▼                                    │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                    3️⃣ 생성된 코드 + 수동 코드                 │  │
│  │                                                              │  │
│  │   🤖 자동 생성              ✍️ 수동 작성                      │  │
│  │   ─────────────            ─────────────                     │  │
│  │   • 서비스 명령어           • CLI 프레임워크                  │  │
│  │   • API 호출 코드           • 인증/설정                       │  │
│  │   • 파라미터 정의           • 출력 포맷터                     │  │
│  │   • 자동완성 기본           • 특수 명령어 (ssh, sync)         │  │
│  │   • 도움말 텍스트           • 에러 핸들링                     │  │
│  │                             • 플러그인 시스템                 │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                │                                    │
│                                ▼                                    │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                    4️⃣ 빌드 & 배포                             │  │
│  │                                                              │  │
│  │   • 크로스 컴파일 (Linux, macOS, Windows)                    │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

## gRPC

### Protobuf 방식 통신 HTTP1.1

```yaml
paths:
    /v1/users/search:
        post:
            requestBody:
                required: true
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/SearchUsersRequest'
                    application/x-protobuf:
                        schema:
                            $ref: '#/components/schemas/SearchUsersRequestBinary'
            responses:
                '200':
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/SearchUsersResponse'
                        application/x-protobuf:
                            schema:
                                $ref: '#/components/schemas/SearchUsersResponseBinary'

components:
    schemas:
        SearchUsersRequest:
            type: object
            properties:
                keyword: { type: string }
                page: { type: integer, format: int32 }
                page_size: { type: integer, format: int32 }
            required: [ keyword ]

        # Protobuf binary 자체는 OpenAPI가 구조를 알 수 없으므로 "binary"로밖에 못 적음
        SearchUsersRequestBinary:
            type: string
            format: binary
        SearchUsersResponseBinary:
            type: string
            format: binary
```

---

## 개발자 경험(DX)을 향상시키는 다양한 도구 (Java, Go, Typescript, Python 등 다양한 언어)

---

## [Kubernetes API Conventions](https://github.com/kubernetes/community/blob/master/contributors/devel/sig-architecture/api-conventions.md)

### `read-modify-write` 왜 GET -> PUT에서 문제가 생기나

시나리오:

1. 클라 A: GET /resource/1 → v1
2. 클라 B: GET /resource/1 → v1
3. 클라 A: 일부 수정 후 PUT → v2 저장
4. 클라 B: 다른 필드 수정 후 PUT → v1 기반 전체 덮어쓰기

→ A의 변경이 조용히 사라짐 (lost update)

#### 낙관적 락으로 해결

- GET 응답에 **버전 정보**를 같이 줌
    - `ETag`
    - `Version`
    - `ResourceVersion`
- PUT 시에 "내가 읽은 버전"을 같이 보냄

```http
PUT /resource/1
If-Match: "v1"
 ```

서버 로직:

- 현재 버전이 v1이면 → 저장 허용
- 이미 v2면 → 409 Conflict / 412 Precondition Failed

→ 덮어쓰기가 발생하기 전에 차단

**표준: ETag + If-Match**

- ETag: "12" (Optimistic Lock Version)

1. GET/HEAD 응답에 `ETag` 포함
2. 클라이언트는 수정할 때 PUT/PATCH에 `If-Match` 포함
3. 서버는 현재 `ETag`와 `If-Match` 비교
4. 불일치면 **412 Precondition Failed** 반환
5. 일치면 업데이트 수행 후 새 ETag 반환

**차선: Last-Modified + If-Unmodified-Since**

- 같은 시각에 여러 변경 발생하면 구분 불가

```http
# 서버 → 클라이언트
Last-Modified: Wed, 21 Oct 2025 07:28:00 GMT

# 클라이언트 → 서버
If-Unmodified-Since: Wed, 21 Oct 2025 07:28:00 GMT
```

### PATCH 메서드

Kubernetes와 같은 현대적인 API 설계에서는 리소스의 전체 교체(PUT) 대신 부분 업데이트(PATCH)를 권장하며, 세 가지 주요
방식을 지원함.

#### 1. JSON Patch ([RFC 6902](https://datatracker.ietf.org/doc/html/rfc6902))

- **방식**: 수행할 작업(operation)의 목록을 JSON 배열로 전달합니다.
- **특징**:
    - `add`, `remove`, `replace`, `move`, `copy`, `test` 작업을 지원합니다.
    - 변경하려는 정확한 위치(JSON Pointer)를 지정해야 합니다.
    - 리스트(Array)의 특정 인덱스를 수정하거나 삭제할 때 유용하지만, 인덱스가 바뀌면 엉뚱한 데이터를 수정할 위험이 있습니다.
- **예시**:
    ```json
    [
      { "op": "replace", "path": "/spec/replicas", "value": 3 },
      { "op": "add", "path": "/metadata/labels/env", "value": "prod" }
    ]
    ```

#### 2. JSON Merge Patch ([RFC 7396](https://datatracker.ietf.org/doc/html/rfc7396))

- **방식**: 리소스의 변경된 부분만 포함된 JSON 객체를 전달합니다.
- **특징**:
    - 직관적이고 구현이 간단합니다. (새로운 값이 들어오면 덮어쓰고, `null`이면 삭제)
    - **단점**: 리스트(Array)를 처리할 때 부분 업데이트가 불가능하고 리스트 전체를 항상 새로 전달해야 합니다.
- **예시**:
    ```json
    {
      "spec": {
        "replicas": 3
      },
      "metadata": {
        "labels": {
          "env": "prod"
        }
      }
    }
    ```

#### 3. Strategic Merge Patch

- **방식**: Kubernetes에서 자체적으로 정의한 방식입니다.
- **특징**:
    - JSON Merge Patch의 단점을 보완하여 리스트 내의 특정 항목을 병합(Merge)할 수 있습니다.
    - 스키마에 정의된 `patchStrategy`와 `patchMergeKey`를 사용하여 병합 로직을 결정합니다.
    - 예를 들어, Pod의 컨테이너 리스트에서 `name`을 키로 사용하여 특정 컨테이너의 이미지 버전만 업데이트할 수 있습니다.
- **예시**:
    ```yaml
    # containers 리스트 전체를 갈아끼우는 게 아니라 name이 "nginx"인 요소만 찾아 수정
    spec:
      template:
        spec:
          containers:
          - name: nginx
            image: nginx:1.14.2
    ```
